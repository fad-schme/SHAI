# tests/fixtures/harness.yaml — test-only config.
version: 1

# Dispatch-token signing key. Required: connectivity is always on.
connectivity:
  token_secret: test-connectivity-secret

scan_input:
  enabled: false

scan_output:
  enabled: false

audit_sinks:
  - name: stdout
