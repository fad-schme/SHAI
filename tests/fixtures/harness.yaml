# tests/fixtures/harness.yaml — test-only config.
# Boundaries disabled; no policy rules file needed.
version: 1

scan_input:
  enabled: false

scan_output:
  enabled: false

policy:
  name: rules

audit_sinks:
  - name: stdout
