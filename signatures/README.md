# This directory is managed by the CLA Assistant workflow.
# The signatures file (cla.json) is created automatically on the first
# signature — do not create it manually, the workflow will fail if it exists.
#
# Directory structure after first signature:
#   signatures/
#   └── cla.json    ← auto-created, contains all contributor signatures